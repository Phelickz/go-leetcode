package main

import "fmt"

func main() {
	// b := containsDuplicate([]int{1, 2, 3, 4})
	// fmt.Println(b)
	// c := maxSubArray([]int{-2, 1, -3, 4, -1, 2, 1, -5, 4})
	// fmt.Println(c)

	// merge([]int{0}, 0, []int{1}, 1)
	d := intersect([]int{4, 9, 5}, []int{9, 4, 9, 8, 4})
	fmt.Println(d)
}
